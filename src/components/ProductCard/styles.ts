import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1 / 2,
    alignItems: 'center',
    borderRadius: 30,
    borderWidth: 1,
    marginHorizontal: 5,
  },
  image: {
    width: 100,
    height: 100,
  },
})
